#loop over upper tri matrix
for i in range(hitnr):
	for j in range(i+1,hitnr):
		a = matrix[i+j]
		if (a > threshold):
clusters_list  = []		
index = 0
for i in range(0,n-1):
	cluster = []
	for j in range(i,n+1):
		if (l[index] ==1):
			cluster.append(l[index])
		if (
		index += 1
	clusters_list.append(cluster)




listoflists = ruhuilrh

import networkx as nx

G=nx.Graph()
G.add_edges_from(listoflists)
list(nx.connected_components(G))

for i in lists...
	for j in that element:
		add up the energies and fill the histogram....


